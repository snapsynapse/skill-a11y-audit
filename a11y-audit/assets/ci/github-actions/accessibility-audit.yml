name: Accessibility Regression Gate

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Build site
        run: npm ci && npm run build

      - name: Scan affected representative templates
        uses: snapsynapse/skill-a11y-audit/.github/actions/scan@v2.8.1
        with:
          serve-path: dist
          port: '8088'
          discover-url: http://127.0.0.1:8088/
          discover-group-map: .a11y-audit/route-group-map.json
          surface-map: .a11y-audit/surface-map.json
          changed-base: ${{ github.event.pull_request.base.sha }}
          changed-head: ${{ github.event.pull_request.head.sha }}
          baseline: .a11y-audit/baseline.json
          fail-on: new
          output: artifacts/a11y-scan.json
          discover-output: artifacts/a11y-discover.json
          selection-output: artifacts/a11y-selection.json

# Adapt the build command, serve-path, and surface map to the target repository.
# Copy route-group-map.example.json and surface-map.example.json into
# .a11y-audit/, then replace their patterns, prefixes, and groups. Unsafe route
# grouping scans every discovered URL; unsafe ownership keeps all selected
# representatives. Create or update the baseline only after human review.
