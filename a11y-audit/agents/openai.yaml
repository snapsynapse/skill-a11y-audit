interface:
  display_name: "Accessibility Audit"
  short_description: "Gate new accessibility regressions"
  default_prompt: "Use $a11y-audit to discover representative site templates, audit them for accessibility issues, and report or gate newly introduced findings against the accepted baseline."
